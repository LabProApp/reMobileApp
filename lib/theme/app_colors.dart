class AppColors {
  static const primary = Color(0xFF1E3A8A);     // Deep Blue
  static const secondary = Color(0xFF0EA5E9);   // Sky Blue
  static const success = Color(0xFF16A34A);     // Green
  static const warning = Color(0xFFF59E0B);     // Amber
  static const danger = Color(0xFFDC2626);      // Red
  static const background = Color(0xFFF8FAFC);  // Light Grey
  static const card = Colors.white;
  static const textDark = Color(0xFF0F172A);
  static const textMuted = Color(0xFF64748B);
}
